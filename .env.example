# GeoManager PROD (EU) — secured login + email MFA
GM_LOGIN_URL=https://eugm.road.com/application/signon/secured/login.html
GM_USERNAME=testvijayautomation
GM_PASSWORD=Trimble@123

# MFA: either a one-shot code (e.g. CI) OR email inbox for dynamic fetch (recommended).
# GM_OTP_CODE=
# Mailinator: local-part or full address (@mailinator.com). Use MAILINATOR_DOMAIN=public for public API.
GM_OTP_EMAIL=vijayakumar919@mailinator.com
MAILINATOR_DOMAIN=public
# MAILINATOR_API_TOKEN=
# MAILINATOR_API_BASE=https://api.mailinator.com/v2

# Optional IMAP instead of Mailinator
# IMAP_HOST=
# IMAP_PORT=993
# IMAP_USER=
# IMAP_PASSWORD=
# IMAP_MAILBOX=INBOX

# OTP polling (defaults match GMloginMFA)
# OTP_MAX_ATTEMPTS=15
# OTP_RETRY_MIN_SEC=3
# OTP_RETRY_MAX_SEC=5

# Optional: absolute or project-relative path to the user definition CSV
# GM_UPLOAD_CSV=define_user (1).csv

# Optional: if the product-tile selector breaks after a UI update
# GM_PRODUCT_TILE_SELECTOR=div:nth-child(4) > div > ...

PLAYWRIGHT_TIMEOUT_MS=90000
PLAYWRIGHT_NAVIGATION_TIMEOUT_MS=90000
